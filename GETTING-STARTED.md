# Getting started / 快速上手

Open one image from Finder, drop an image onto Piclune, or choose File → Open Folder. The current folder becomes the browsing context; files do not need to be imported.

| Action / 操作 | Shortcut / 快捷键 |
|---|---|
| Previous / Next · 上一张／下一张 | ← / → |
| Fit to Window · 适合窗口 | ⌘0 |
| Actual Size · 实际大小 | ⌘1 |
| Fit Width / Height · 适合宽度／高度 | ⌘2 / ⌘3 |
| Folder Overview · 文件夹总览 | ⌘G |
| Slideshow · 幻灯片播放 | ⇧⌘F |
| Leave overview / slideshow · 返回 | Esc |
| Settings · 设置 | ⌘, |
| Move to Trash · 移到废纸篓 | ⌘⌫ |

The scroll wheel zooms by default. Pinch to zoom on a trackpad; when zoomed in, drag or use two fingers to pan. In overview, click once to select and again to open. File → Open Folder is useful if a file's surrounding folder cannot be read.

New-install defaults: Liquid Glass (with OS fallback), dark appearance, pinned toolbar and bottom thumbnails, bottom image summary, hidden inspector, 14 pt fit margins, 70 pt thumbnails, waterfall overview, 0.30 s slide transitions. Existing users keep their saved preferences. Reduce Motion takes precedence.

中文：从访达打开图片、拖入图片，或选择“文件 → 打开文件夹”。无需导入图库。滚轮默认缩放，触控板双指捏合缩放；放大后拖动或双指滑动平移。总览中单击选择，再次单击打开。若无法读取文件周围的目录，可直接选择打开文件夹。

新用户默认：液态玻璃（旧系统回退）、深色、固定工具栏与底部缩略图栏、底部概览、隐藏信息栏、14 pt 适配边距、70 pt 缩略图、瀑布流总览、0.30 秒滑动动画。已有用户保留偏好。系统“减少动态效果”优先。
